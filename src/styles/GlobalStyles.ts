import { StyleSheet } from 'react-native'

export const globalStyles = StyleSheet.create({
    title: {
        color: '#666',
        fontSize: 24,
    },
    text: {
        color: '#999',
        fontSize: 14,
    },
    link: {
        color: '#56c143',
        fontWeight: 'bold',
        fontSize: 14,
    }
})